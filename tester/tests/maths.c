// Test avec une bibliothèque qui utilise une bibliotheque dynamique
#include <stdio.h>
#include <math.h>

int main() {
    double num = 9.0;
    double result = sqrt(num);
    printf("The square root of %.2f is %.2f\n", num, result);
    return 0;
}
